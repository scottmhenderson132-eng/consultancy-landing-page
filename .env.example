# Public URL of the QBO MCP server authorize endpoint, e.g. https://qbo.sh-advisory.ca/authorize. Public value, not a secret. Leave empty to render the connect action as disabled.
NEXT_PUBLIC_QBO_AUTHORIZE_URL=
